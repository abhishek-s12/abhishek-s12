# README Auto Refresh
name: Refresh README

on:

  workflow_dispatch:

  schedule:

    - cron: "0 0 * * *"

jobs:

  refresh:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - run: echo "README refreshed"
